int fib(int n) {
    if (n <= 1) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

int add3(int a, int b, int c) {
    return a + b + c;
}

int main() {
    int f5 = fib(5); // 0, 1, 1, 2, 3, 5 -> 5
    int sum = add3(1, 2, 3); // 6
    return f5 + sum; // 5 + 6 = 11
}

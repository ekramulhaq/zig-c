int main() {
    int arr[10];
    int sum = 0;
    for (int i = 0; i < 10; i = i + 1) {
        arr[i] = i + 1;
    }

    for (int i = 0; i < 10; i = i + 1) {
        sum = sum + arr[i];
    }

    // Sum of 1 to 10 is 55
    if (sum == 55) {
        return 42;
    }
    return 0;
}
